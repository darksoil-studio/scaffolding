{{#if (eq cardinality "vector" )}}
<search-users name="{{snake_case field_name}}" .label=${msg("{{label}}")}{{#if required}} required{{/if}}{{#if edit}} .defaultValue=${[{{default_value}}]}{{/if}}></search-users>
{{else}}
<search-user name="{{snake_case field_name}}" .label=${msg("{{label}}")}{{#if required}} required{{/if}}{{#if edit}} .defaultValue=${[{{default_value}}]}{{/if}}></search-user>
{{/if}}
